const playersData = require("./players");
const teamsData = require("./teams");
const users = require('./users')

module.exports = {
    players: playersData,
    teams: teamsData,
    users: users
};
